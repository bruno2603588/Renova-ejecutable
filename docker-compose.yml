version: '3.8'

services:
  backend:
    build: .
    ports:
      -"8000:8000"
    environment:
      - DATABASE_URL=sqlite:///./renova.db
    volumes:
      - sqlite_data:/app/data

volumes:
sqlite_data:
